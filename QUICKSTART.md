# راهنمای شروع سریع

## 📋 فهرست محتویات

1. [نمونه اولیه](#نمونه-اولیه)
2. [باز کردن وبسایت](#باز-کردن-وبسایت)
3. [سفارشی‌کردن](#سفارشی‌کردن)
4. [فیچرهای اصلی](#فیچرهای-اصلی)

---

## 🎯 نمونه اولیه

این وبسایت شامل:

```
✓ هدر حرفه‌ای با منوی همبرگر
✓ بخش hero جذاب
✓ مودال‌های interactive
✓ نمایش دوره‌های آموزشی
✓ فوتر کامل
✓ طراحی responsive
✓ انیمیشن‌های جالب
```

---

## 🚀 باز کردن وبسایت

### روش 1: مستقیم از فایل
```bash
# فایل index.html را در مرورگر باز کنید
open index.html
```

### روش 2: با سرور محلی (بهتر است)

#### با Python 3:
```bash
cd /Users/dc01014/Documents/MitraWebsite
python -m http.server 8000
```

#### با Python 2:
```bash
cd /Users/dc01014/Documents/MitraWebsite
python -m SimpleHTTPServer 8000
```

#### با Node.js:
```bash
# ابتدا http-server را نصب کنید
npm install -g http-server

cd /Users/dc01014/Documents/MitraWebsite
http-server
```

سپس در مرورگر وارد شوید:
```
http://localhost:8000
```

---

## 🎨 سفارشی‌کردن

### تغییر رنگ‌ها
در فایل `styles.css` بخش `:root` را پیدا کنید:

```css
:root {
    --primary-color: #8B4513;      /* رنگ قهوه‌ای */
    --secondary-color: #D4AF37;    /* رنگ طلایی */
    --accent-color: #C41E3A;       /* رنگ قرمز */
    --text-color: #2c3e50;         /* رنگ متن */
    --light-bg: #f5f1e8;           /* پس‌زمینه روشن */
    --dark-bg: #1a1a1a;            /* پس‌زمینه تیره */
}
```

### تغییر فونت‌ها
برای استفاده از فونت‌های فارسی بهتر، این کد را به `<head>` اضافه کنید:

```html
<link href="https://fonts.googleapis.com/css2?family=Vazirmatn:wght@400;500;600;700&display=swap" rel="stylesheet">
```

### تغییر متن‌ها
تمام متن‌های فارسی در فایل `index.html` هستند. آنها را مستقیماً ویرایش کنید.

### تغییر تصاویر
1. **Hero Background**: خط 95 در `index.html`
2. **Course Images**: خطوط 168-183 در `index.html`

---

## 📱 فیچرهای اصلی

### 1️⃣ منوی همبرگر
- به‌طور خودکار در موبایل فعال می‌شود
- برای دسکتاپ نمایش داده نمی‌شود
- انیمیشن روان برای باز/بسته‌کردن

### 2️⃣ مودال‌های پاپ‌آپ
- **عضویت**: فرم کامل برای ثبت‌نام
- **بیشتر بخوانید**: نمایش متن کامل
- **ورود/ثبت‌نام**: صفحات جداگانه

### 3️⃣ دوره‌های آموزشی
- 4 دوره نمونه
- کارت‌های جذاب
- انیمیشن هنگام اسکرول

### 4️⃣ طراحی Responsive
- **موبایل**: تک ستون
- **تبلت**: دو ستون
- **دسکتاپ**: چهار ستون

---

## 🔧 ویرایش سریع

### تغییر عنوان صفحه
فایل `index.html`:
```html
<title>عنوان جدید</title>
```

### تغییر لوگو
فایل `index.html`:
```html
<h1 class="logo">نام جدید</h1>
```

### تغییر متن Hero
فایل `index.html`:
```html
<h2 class="hero-title">متن جدید</h2>
```

### اضافه کردن دوره جدید
فایل `index.html` - بخش courses:
```html
<div class="course-card">
    <div class="course-image" style="background-image: url('image.svg');"></div>
    <h3>نام دوره</h3>
    <p class="course-summary">توضیح کوتاه</p>
    <button class="course-btn">بیشتر بخوانید</button>
</div>
```

---

## 📧 اتصال فرم‌ها

برای ارسال داده‌های فرم به ایمیل یا سرور، به `script.js` رفته و بخش `FORM SUBMISSIONS` را تغییر دهید:

```javascript
document.querySelectorAll('.signup-form, .login-form').forEach(form => {
    form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // داده‌های فرم را اینجا ارسال کنید
        const formData = new FormData(this);
        
        // مثال: ارسال به سرور
        fetch('/submit-form', {
            method: 'POST',
            body: formData
        });
    });
});
```

---

## 🐛 حل‌مسائل

### تصاویر نمایش داده نمی‌شوند
- از سرور محلی استفاده کنید (روش 2 بالا)
- مسیر تصاویر را بررسی کنید

### مودال‌ها کار نمی‌کنند
- بررسی کنید که `script.js` بارگذاری شده است
- Console را بررسی کنید (F12)

### متن فارسی کج است
- تایید کنید که `dir="rtl"` در `<html>` وجود دارد
- فونت را بررسی کنید

---

## 📚 منابع مفید

- [MDN Web Docs](https://developer.mozilla.org)
- [CSS Tricks](https://css-tricks.com)
- [JavaScript Info](https://javascript.info)

---

## ✨ بهتری‌های آتی

- [ ] صفحات داخلی برای دوره‌ها
- [ ] سیستم نظرات
- [ ] پنل مدیریتی
- [ ] صفحه درباره ما
- [ ] فرم تماس
- [ ] جستجوی دوره‌ها

---

**آماده‌ای؟ سایت را باز کن و لذت ببر! 🎉**
